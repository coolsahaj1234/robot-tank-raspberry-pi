{
  "name": "web-robot-controller",
  "version": "1.0.0",
  "description": "React web app to control robot tank via old_robot TCP server",
  "main": "server/index.js",
  "scripts": {
    "start": "node start.js",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run ai-service\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "ai-service": "cd ai_service && source venv/bin/activate && python3 server.py",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install",
    "install-ai": "cd ai_service && pip3 install -r requirements.txt"
  },
  "keywords": ["robot", "tank", "control", "react"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "net": "^1.0.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

